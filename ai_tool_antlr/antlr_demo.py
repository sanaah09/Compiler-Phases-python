# Demonstration of using ANTLR-generated lexer and parser
# (Code generation step is omitted for simplicity)

print("ANTLR is used to generate lexer and parser automatically using grammar rules.")
print("Grammar file: Expr.g4")
print("Input example: a = b + 10;")
print("ANTLR validates syntax based on grammar.")
